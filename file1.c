

#include "file1.h"

/*
---------------------------
FUNCTION monkey_trouble
---------------------------
We have two monkeys, a and b, and the parameters a_smile and b_smile indicate if each is smiling.
We are in trouble if they are both smiling or if neither of them is smiling. Print whether we are in trouble or not.
monkey_trouble(True, True) ? True
monkey_trouble(False, False) ? True
monkey_trouble(True, False) ? False
*/

void monkey_trouble(int a_smile, int b_smile) {
	// 1. Set the variable to True or to False, whatever you prefer

	// 2. Check the first case different to your initial value
	
	// 3. Check the second case different to your initial value

	// 4. Print the result
}

/*
--------------------------------
 FUNCTION sum_double
--------------------------------
Given two int values, print their sum. Unless the two values are the same, then print double their sum.
sum_double(1, 2) ? 3
sum_double(3, 2) ? 5
sum_double(2, 2) ? 8
*/

void sum_double(int a, int b) {
	// 1. Create a variable to store the sum

	// 2. Check the special case where they are equal, so as to double the sum then.

	// 3. Print the sum
}

/*
----------------------------
 FUNCTION near_hundred
----------------------------
Given an int n, print whether it is within 10 numbers away from 100 or 200.
near_100_or_200(93) ? True
near_100_or_200(105) ? True
near_100_or_200(89) ? False
*/

void near_100_or_200(n) {
	// 1. Create a variable 'close', initially set to value 'False'

	// 2. Check if 'n' is close to 100
	// 2.1.If n is smaller than 100

	// 2.2.If n is bigger or equal than 100

	// 3. Same reasoning for 200
	// 3.1.If n is smaller than 100

	// 3.2.If n is bigger or equal than 100

	// 4. Print the result
}
